import './firebase-auth.module.scss';

/* eslint-disable-next-line */
export interface FirebaseAuthProps {}

export function FirebaseAuth(props: FirebaseAuthProps) {
  return (
    <div>
      <h1>Welcome to firebase-auth!</h1>
    </div>
  );
}

export default FirebaseAuth;
